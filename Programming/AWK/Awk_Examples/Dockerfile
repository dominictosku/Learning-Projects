# Use the official Alpine Linux as a base image
FROM alpine:latest

# Create a directory to serve as the mount point
RUN mkdir /dockermount

# Set the working directory to the mount point
WORKDIR /dockermount

# The entry command or process to run when starting the container
CMD ["sh"]